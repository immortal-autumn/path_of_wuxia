# Project Functions

This is the canonical inventory of implemented project behavior. Update it in the same change whenever a function is added, changed, renamed, or removed.

## Application entries

| Entry | Function | Persistence / real-time | Automated coverage |
| --- | --- | --- | --- |
| `/` | Loads the shared-world game from玄关 and renders China time, map/fast travel, resident interactions/commissions, actions, character/skills/items, `贯/文` wallet, Song retail/guild markets, Kaifeng law, combat/cultivation and chat. | Reads the SQLite world and authoritative progression/economy/law, settles elapsed systems, and performs real-time commands over `/ws`. | Unit world/economy/market/NPC/law/progression tests and Playwright map, person, shop/market/law, action, multiplayer, responsive and mobile tests. |
| `/map-editor` | Loads the responsive collaborative drag/drop designer for the continuous overworld and exceptional maps, with a concise canvas-status bar, text-only rectangular locations, chunk controls, inspector, locks, and history. | Reads bounded map viewports and writes through authenticated WebSocket commands. | Playwright fifteen-seed-map inventory, responsive layout, drop, route, undo/redo, region-drag, and lock-contention tests. |
| `/action-editor` | Loads the boxed responsive structured action-rule designer with template list, full validated rule form, location search and facility bindings; production entry requires an editor/admin role. | Reads/writes action templates and bindings through authenticated `/ws`; server-side authorization protects mutations and rule invalidations refresh game clients. | Unit CRUD/version/safety/seed-preservation/cost-validation tests and Playwright create/bind/execute/delete flow. |
| `/api/session` | Restores or creates a random player, or binds a trusted reverse-proxy OIDC subject to an existing player, then issues an HttpOnly browser session and performs an origin-checked redirect that rejects external and backslash-based targets. Cookie-less identity creation is rate-limited per forwarded client address. | Writes `players`, `sessions`, `external_identities`, and an arrival event; trusted editor/admin claims update the persisted editor role. | Unit identity/external-subject/role/redirect/rate-limit tests and Playwright entry test. |
| `/api/health` | Returns a non-cacheable readiness response for the application and SQLite connection. | Performs a bounded `SELECT 1`; returns HTTP 200 when ready and 503 on database failure without exposing game data. | Playwright HTTP readiness test and production smoke check. |
| `/ws` | Authenticates browser sessions, allowlisted gameplay NPC credentials, or isolated market-trade credentials. Browser editor mutations require an editor/admin role; gameplay NPCs are limited to sync/movement/action/skill/combat/loot/chat commands; market-trade sockets receive only restricted `agent.trade.*`. | Uses SQLite transactions, bounded 32KB payloads, per-socket command budgets and slow-client backpressure while broadcasting incremental invalidations and excluding trade agents from snapshots, directives, public broadcasts and online counts. | Live smoke and Playwright real-time/editor/gameplay-NPC allowlist/restricted-trade/rate-limit tests. |
| App Router error recovery | Segment `app/error.tsx` | Replaces the default Next.js failure screen with a boxed recovery view that logs the client error and offers retry or return-to-game controls without mutating persisted state. | Type/build validation and Playwright forced-error recovery coverage. |
| Global error recovery | App `global-error.tsx` | Covers root-layout failures with a standalone accessible HTML recovery page and retry/home controls. | Type/build validation and production error-boundary smoke check. |

## Player and world functions

| Function | Interface | Behavior | Automated coverage |
| --- | --- | --- | --- |
| Automatic identity | First browser visit | Creates a collision-safe random identity at `home-entrance`; browser contexts remain distinct. | Unit 2,000-name test and Playwright identity test. |
| China-standard time | World panel / `world.updated` | Displays an `Asia/Shanghai` `YYYY-MM-DD HH:mm:ss` clock synchronized from server time. | Unit deterministic-time and Playwright world tests. |
| Skill-scaled neighborhood | Game map / character “技能” / `skill.use`, `skill.stop` | Queries only the current-map ordinary-route graph at base depth 3. Eagle Eye activates immediately, enters a persisted 30-minute cooldown, and on success grants a separately persisted 30-minute effect up to depth 8. Natural expiry or active stop removes the bonus immediately without clearing cooldown; temporarily seen locations never enter exploration history. | Unit Eagle Eye immediate activation/duration/stop/cooldown/range/exploration tests and Playwright skill-detail expanded-vision/stop test. |
| Persistent exploration history | Successful movement / `map.visited` | Records the initial location and every successfully reached destination per opaque player in `player_visited_locations`. The on-demand query returns only active locations this player has visited, only connections whose endpoints were both discovered, only maps containing those locations, and the intersection of visited locations with active fast-travel facilities; schema v6 backfills existing players from movement logs without mixing identities. | Unit initial-map, discovered-route, unlocked-hub, revisit deduplication and per-player isolation tests; Playwright visited-map request test. |
| Personal visited-map viewer | Desktop “足迹地图” / mobile “足迹” buttons | Opens a fitted overview of every location reached by the current player. It switches between visited maps, provides bounded zoom and fit-to-full-map controls, draws only discovered same-map connections, marks current position/unlocked travel hubs, and exposes full names/areas/coordinates outside compact nodes. A hub dropdown and explicit second-step button can start fast travel, but undiscovered places never appear or become selectable. | Playwright initial secrecy, movement growth, map switching, zoom/fit, route, inspection, unlocked-hub selection, travel, close and mobile-button tests. |
| Concise map labels | Game and editor location rectangles | Derives a map-only label of at most nine characters by removing repeated house/region/category prefixes and compacting generated cave/memo names. Canonical full names remain unchanged in SQLite and commands; full names plus coordinates remain available through accessibility/status text, editor selection and inspector fields. | Unit exact-label/truncation tests and Playwright compact-node/full-status tests. |
| Structured local-map depth | Game map stage | Organizes the currently visible neighborhood with data-derived region planes, a coordinate scaffold, a four-way orientation marker, thick foreground one-step roads, thin distant roads, numbered step markers, hard-offset rectangular node depth, and distance-based foreground/midground/background strokes. Nodes still show names only, Qinggong keeps its teal double border, no background image is used, and rendering does not alter topology, visibility, or movement rules. | Playwright region/grid/route-depth/node-depth/orientation assertions, rectangular-size/name-overflow checks, movement checks, and mobile overflow coverage. |
| Geographic public overworld | `world-root` ordinary-route graph | Places the modern house exterior and three same-named 楼门路 segments beside a 66×70-grid historical Tokyo Kaifeng city; the east road now terminates at a single “东境建设中” node. Kaifeng keeps 935 public locations and 975 ordinary city routes. Palos and its 510 locations are inactive and absent from all player views. | Unit exact Kaifeng anchors, city span, loop density, reciprocal slots, removed-Palos and construction-boundary validation; Playwright Kaifeng axis walk and construction-boundary tour. |
| Song storefront geography | 120 existing Kaifeng street cells | Reuses stable street location IDs and routes while replacing selected 马行街、潘楼街、汴河、朱雀门、浚仪桥、梁门、东水门与东西华门街 labels with 120 inferred Song storefronts across medicine, food, tea, textile, finance, lodging, book and craft categories. Full descriptions explicitly distinguish historically documented relationships from project-inferred shop names. | Unit exact storefront count, shop facilities, source coverage and preserved road topology; Playwright editor shop locate. |
| Authoritative `贯/文` wallet | Character footer / all money mutations | Stores integer `cash_wen` separately from the retired silver column, formats `1贯=1000文`, and records every post-migration gain/loss with its resulting balance in an immutable per-player ledger. Schema v10 migrates old silver, loot, custom action outcomes and active trade offers exactly once at `1银两=1贯`. | Unit exact formatter, v9 migration/idempotency, legacy-column isolation, action/trade/loot and ledger tests. |
| Finite-stock Song retail | Current shop card / `shop.inspect`, `shop.buy`, `shop.sell` | Seeds 120 persistent shops and 168 storefront/interior service points. Each has China-time hours, finite stock and a finite till; buys and sells validate location, opening time, wallet, stock, binding/equipment/reservations and then update wallet, ledger, stock, till, inventory and transaction record in one SQLite transaction. Replayed request IDs cannot double-charge or carry a different payload. | Unit count, hours, buy/sell, insufficient-funds, location, reseed persistence and idempotency tests; Playwright shop modal buy/sell flow. |
| Shop detail modal | Shop card and shop item buttons | Opens a keyboard/backdrop/Close-dismissible dialog with full shop status, hours, wallet/till, product descriptions, finite quantities, buy/sell quotes and explicit quantity confirmation. | Playwright desktop/mobile detail, no-mutation close, buy and sell tests. |
| Guild price-time order book | Market facility / `market.snapshot`, `market.order.place`, `market.order.cancel` | Trades ten 文-denominated commodity underlyings through persistent player and guild-maker limit orders. Highest bid/lowest ask then creation sequence determines matches; partial fills remain cancellable, and placement/cancellation request IDs are replay-safe. Cash/premium/order-margin reservation is global across instruments, cannot be spent in retail or direct trade, and spot selling requires uncommitted holdings. Superseded untraded guild quotes are deleted instead of accumulating forever. | Unit ten-underlying/exact-100-active-contract seed, 200 guild quotes/retention, maker/partial fill, cross-instrument reserve, retail isolation and cancellation replay tests; Playwright spot fill/resting cancel flow. |
| Futures and European options | Market order ticket | Maintains exactly one active 1/7/30-China-calendar-day future/call/put series per underlying, expiring at 20:00 Asia/Shanghai, for exactly 100 active instruments including spot. Futures support long/short positions; calls and puts support buying and writing. Expired futures mark to spot, options cash-settle intrinsic value, then only the expired series is replaced. | Unit exact expiry, 31-day exact active count, long/short, call writing and real-time European settlement tests. |
| Market margin, marks and liquidation | Market snapshot / autonomous 60-second server settlement | Deterministically marks ten underlyings at most once per minute even with no market viewer, transfers futures variation margin, requires 20%/10% initial/maintenance futures margin and 25%/15% written-option margin, automatically closes risky positions, audits debt creation/repayment and blocks new orders while clearing debt remains. Open market modals refresh on `market.updated`. | Unit tick deduplication, global reservation, forced liquidation, debt ledger/blocking and autonomous service tests. |
| Market detail modal | “打开市易行会” action card | Shows all underlyings, mark/bid/ask, contract kind and expiry, multiplier, wallet/margins/debt, long/short positions and open orders; uses explicit 文 price/quantity confirmation and cancel controls, with Escape/backdrop/Close support. | Playwright desktop/mobile market browse, spot trade and cancel tests. |
| Enterable Kaifeng buildings | Street landmark transitions / `move` | Keeps the capital street map flat while making five public complexes and eight representative shops enterable. All are non-nested single-floor maps containing 89 total rooms, halls, courtyards and galleries. | Unit exact 15-layer inventory, rooms, transitions, facilities and map validation; Playwright enters Xuande Gate and walks the palace interior. |
| Eight-direction movement | Map box / `move` | Ordinary routes connect one adjacent grid cell in eight directions; movement and ordinary actions never check or consume endurance. | Unit free-movement/direction tests and Playwright seed-tour tests. |
| Server-authoritative fast travel | Visited map / `travel.fast` | Instantly moves an idle, undefeated, non-combat player only to a personally visited active hub. Home, Kaifeng entrances/gates and State Bridge remain hubs; construction and removed Palos locations are never eligible. | Unit visited/hub/action-lock/log/event validation and Playwright visited-map travel flow. |
| Explicit direction controls | Boxed “下一步” footer | Lists each currently adjacent direction and destination outside the SVG, disables movement while offline or pending, and sends the same validated `move` command as a rectangular map node. | Playwright rectangular-node and direction-control test. |
| Plain-HTTP browser compatibility | Game and map-editor commands | Generates collision-resistant client request/entity IDs with `crypto.getRandomValues` and a legacy fallback, so movement and editing work when `crypto.randomUUID` is unavailable. | Playwright no-`randomUUID` movement and editor tests. |
| Interior transitions and portals | Action panel / `move` | The bundled world uses one house door plus thirteen Kaifeng landmark/shop entrances between the flat overworld and complete single-floor interiors. Doors, gates, stairs and portals remain available for user-created exceptional maps. | Unit same-layer/cross-layer/building-transition tests and Playwright house, palace-interior and overworld tours. |
| Six-attribute progression | Character tabs / `attributes.allocate` | Stores strength, agility, constitution, root, comprehension and spirit; atomically spends permanent points and recalculates transparent combat values. | Unit formulas/scaling/atomic-allocation tests and Playwright allocation test. |
| Cultivation and breakthroughs | Training locations / `cultivation.breakthrough` | Settles unlimited online/offline cultivation from server time, auto-levels within a realm, and applies 45/60/75/100% breakthroughs with failure cost and 30% carry. | Unit clock/idempotency/failure/success/reward tests and Playwright training/offline/100% breakthrough tests. |
| Derived combat panel | Character “战斗属性” tab | Computes HP, endurance, min/max attack, defense, speed, hit, dodge, critical rate/damage and cultivation speed from attributes and realm. | Unit deterministic-formula and Playwright derived-panel tests. |
| Active and passive skill panel | Character “技能” tab / `skill.use`, `skill.stop` | Groups every learned skill by persisted `active`/`passive` kind and shows linked attribute, level, experience, configured effect duration, live remaining duration and cooldown. Active self skills resolve immediately without entering the action queue; successful timed effects persist in `player_active_skills` and can be stopped early without resetting cooldown. Unavailable rules are disabled, Qinggong uses map landing borders, and direct `action.start` cannot bypass skill rules. | Unit skill-kind metadata, passive rejection, duration/expiry/stop, queue bypass and cooldown persistence tests; Playwright skill detail modal, Eagle Eye activation/stop and cooldown controls. |
| Character detail modals | Skill and inventory entries/buttons | Opens a keyboard-accessible modal before a skill or item mutation. Skill details show type, attribute, level/XP, configured duration, live effect and cooldown before confirm-use/stop; item details show description, category, quantity/reservation, quality, durability, binding, slot and effects before confirm-equip/unequip/use. Escape, backdrop and Close dismiss without mutation. | Playwright skill and item modal content plus confirmed activation/stop/equipment tests. |
| Real-time action queue | Action panel / `action.start`, `action.cancel`, `action.queue.reorder` | Runs exactly one foreground action and up to eight ordered waiting actions with real-world durations. Accepted jobs persist an immutable rule snapshot/version, enforce facilities, item/cash/HP costs and ordinary cooldowns, reserve item inputs, release them on cancellation, settle per-job failures into a terminal state, and continue by server time offline without endurance costs. | Unit start/cap/reorder/cancel/movement-lock/server-clock/offline-completion/cost/facility/cooldown/snapshot/failure-isolation tests and Playwright queue controls/countdown test. |
| Living needs | Action panel and player snapshot | Persists 0–100 satiety, hydration, hygiene, fatigue and bladder values; decays them by elapsed server hours and applies a visible 5% check penalty per poor need capped at 25%, without death or basic-movement costs. | Unit deterministic eight-hour decay, action recovery and maximum-penalty tests; Playwright needs summary test. |
| Reusable facility actions | Seed revision 10 / location facilities | Gives every active location observation/listening, preserves house facilities, tags Kaifeng streets/gates/bridges as roads, the capital and all interiors as settlements, and all 120 storefronts plus shop rooms with `market`/`shop` facilities. | Unit Kaifeng road/market/shop/building facility availability and seeded action-catalog tests; Playwright location-action execution test. |
| Private perception results | `action.start` observation/listening and `skill.use` Eagle Eye | Observation returns the current description and facilities; listening returns at most three recent nearby public non-adult action traces or a quiet result. Eagle Eye resolves immediately. All three results persist only in the actor's `private_events`, appear in personal action history, and never broadcast later private/adult records. | Unit visibility/filter/result tests and Playwright private-result test. |
| Instant map-border Qinggong | Double-bordered map nodes / `qinggong.start` | Renders every existing active same-layer location two to a skill-scaled seven grid cells away on one of eight straight directions, including valid targets outside ordinary route vision. Clicking or keyboard-activating the special border resolves immediately without an action job and starts a persisted 60-second cooldown on success or failure. Routes are not required; only success moves and records exploration, while failure applies configured HP/fatigue costs. Cooling targets remain visible with a disabled dashed border and countdown/status details. | Unit geometry/range/immediate success/failure/cooldown persistence/no-queue/exploration tests and Playwright special-border movement/cooling test. |
| Direct player interactions and relationships | Character “交往交易” / `interaction.greet/request/respond`, `relationship.end`, `player.block` | Targets only same-location online players for greetings and five-minute mutual requests. Accepted friend, sworn, mentor/disciple, lover and spouse relationships persist symmetrically; either participant can end one. Blocking prevents direct interaction and cancels pending requests/trades. | Unit relationship roles/lifecycle/block tests and Playwright two-user relationship negotiation. |
| Per-action adult consent and privacy | Adult settings / `profile.adult.update`, `interaction.request/respond` | New humans remain unknown-age with adult content off. An adult participants-only action requires both characters confirmed adult and opted in at request, acceptance and completion; every action needs a fresh explicit five-minute request, same-location presence and online acceptance. Minor/unknown/disabled/coercive paths are rejected, disabling eligibility cancels pending requests and accepted/running adult jobs without rendering adult result text, and valid results persist only to both participants. | Unit unknown/minor/opt-in/fresh-consent/revocation/privacy/completion tests and Playwright two-user consent flow. |
| Atomic direct trade | `trade.request/respond/offer/confirm/cancel` | Same-location online players negotiate 文-denominated cash and up to16 item stacks. Bound, equipped, missing or action-reserved quantities are rejected; quote changes clear both confirmations, and final confirmation exchanges wallet funds/items atomically with ledger entries. | Unit two-confirmation, partial-stack, cash, binding, ledger and completion-event tests; Playwright two-user quote/confirm trade. |
| Authoritative turn combat | Character attack / `combat.start`, `combat.choose` | Allows PvP at every location without safe zones when both same-location players are online and idle. The server owns alternating30-second turns with attack, lower-hit higher-damage power attack, one-hit defend reduction and speed-based flee; non-turn choices and all ordinary actions/movement during combat are rejected. | Unit turn ownership, damage/defend, movement lock and state tests; Playwright two-user turn handoff. |
| Combat timeout and auto-flee | One-second server combat clock | A missed deadline records an automatic defend and increments that actor's missed-turn counter. A manual choice resets that actor's counter; three consecutive misses end combat by automatic flee without defeat loot, including while clients are offline. | Unit multi-deadline settlement/turn-log/no-loot tests and Playwright forced-deadline auto-flee. |
| Full defeat loot and respawn | Combat completion / `loot.take`, human `combat.respawn` | HP-zero defeat atomically moves personal `cash_wen` through the ledger into a location loot pile and drops every unbound personal item/equipment while bound items remain. Humans manually return to玄关 at half HP; defeated NPCs cannot invoke manual respawn and instead return once after exactly ten minutes at the due-time workplace or home with half HP. Shop stock/till are independent employment assets and never enter owner loot. | Unit human defeat/respawn, NPC 9:59/10:00 delayed return, personal loot and unchanged shop stock/till tests. |
| Persistent NPC population | SQLite startup | Idempotently creates exactly240 stable resident actors and assigns 120 shopkeepers, 24 assistants/artisans, 24 commerce workers, 16 constables, 16 patrol/guards, 16 specialists and 24 townsfolk to active Kaifeng homes and workplaces. One shopkeeper is linked to each persistent shop; six daily China-time schedule slots, eight shared city routes and exactly72 public resident relationships are seeded without resetting mutable player state on restart. Public player/presence shapes omit controller type and private profile fields. | `tests/npc-service.test.ts` exact cohort/subtype/shop/workplace/schedule/relationship and public-shape tests. |
| Credentialed scheduled NPC actors | `/ws` Bearer authentication / private `npc.directive` / `npm run npc:start` | Each gameplay runner actor refreshes its snapshot before deciding, gives defeat/combat priority, then follows a server-computed China-time move/action/hold directive one legal adjacent route hop at a time before utility fallback. Directives go only to the matching credentialed NPC socket and never enter browser snapshots or public presence. | Focused schedule resolver tests, TypeScript build and Playwright credentialed NPC route test. |
| NPC person detail and local strip | Map footer “此地人物” / `person.inspect`, `person.dialogue.choose` | Shows same-location online people outside map nodes. The accessible detail modal exposes only public identity, occupation, biography, workplace, current activity, personal standing, seeded topic choices and public relationships; controller identity, credentials, personality, goals and route directives remain private. Greeting standing can rise only once per China calendar day. | `tests/npc-service.test.ts` same-location, public privacy and daily standing-cap tests; Playwright modal open/dialogue/Escape/backdrop/Close coverage. |
| Atomic NPC commissions | Character “委托” / `commission.accept/complete/abandon` | Seeds one offer per resident across visit, action and deliver objectives. Acceptance snapshots objective/reward and is request-idempotent; progress derives from visited-location timestamps, completed action logs or eligible unbound/unreserved inventory. Completion at the issuing resident atomically consumes delivery goods, credits the wallet ledger, changes standing and records a private event; expiry, abandon and completion replay cannot duplicate rewards. | `tests/npc-service.test.ts` accept/replay/progress/atomic reward/ledger tests and Playwright journal/return flow. |
| Scoped reproducible NPC trade agents | `npm run npc:trade:start` / restricted `agent.trade.*` | Seeds a separate `market-trade` credential per NPC. Each hidden immutable strategy is deterministic by default or strictly installed from an optional bounded HTTP response. Every cycle persists only the strategy's relevant spot quotes plus available cash/positions and complete open-order reservations; output persistence and market execution are atomic. Trade sockets cannot move/chat/fight/edit, receive no public snapshot/directive and do not affect presence; `npc:trade:replay` opens SQLite read-only and exits nonzero on tampering. | Pure strategy/hash/replay/available-assets tests, atomic persistence/idempotency tests and Playwright restricted socket decision flow/privacy test. |
| Kaifeng wanted law | Combat/loot / world “开封法度” / `law.surrender` | Only human crimes against NPC residents in the Song/Kaifeng jurisdiction add wanted points: assault +20, defeat +50 and robbery +50, each reference-idempotent. Wanted decays one point per complete hour; shops refuse buy/sell at20, constables privately pursue at50, and the detail modal shows thresholds/history without putting law state on map/person cards. At the府署正堂 an idle living player can explicitly confirm a `wanted×100文` atomic fine to clear the record. | Pure boundary/decay tests, service incident/refusal/pursuit/surrender/replay/ledger tests and Playwright shop refusal/law modal/surrender flow. |
| NPC controller boundary | `UtilityNpcController`, `HttpNpcController`, `NPC_CONTROLLER_URL` | Default utility AI prioritizes combat and needs around mandatory schedule directives. An optional HTTP adapter sends actor context to an external/LLM controller and rejects responses outside a narrow command whitelist. | Unit deterministic utility-decision test and TypeScript/build validation of both adapters. |
| Complete item instances and equipment | Character “物品装备” detail modal / `inventory.equip`, `inventory.unequip`, `inventory.use` | Persists stackable resources and unique 1–5-quality item instances with binding, affixes, durability and equipment slots. Stable bound starter clothing, boots, weapon, tool, food and water are idempotently granted to existing/new actors; equipped effects modify derived combat stats, consumables atomically change needs/HP, and action-reserved quantities cannot be used or equipped. Mutations require the matching confirmation control in the item detail modal. | Unit starter-idempotency, equip/stat, consumable and reservation tests; Playwright item modal and inventory confirmation controls. |
| Recipe crafting | Current-facility recipe cards / `craft.start` | Exposes only recipes supported by the current kitchen/workshop/smithy, displays inputs, outputs, difficulty and real duration, reserves stack quantities when queued, releases them on cancel, and consumes them atomically before granting success output and skill XP. | Unit 30-minute server-clock crafting, cancel-release, input-consumption and stack-output tests; Playwright recipe visibility test. |
| Persistent real-time farming | Garden farm cards / `farm.start` | Gives each seeded farm facility a persistent plot. Planting reserves one seed for a 30-minute action, crops then grow independently for realistic 30/120-day periods, watering improves plot state, and a 30-minute mature harvest grants yield based on water/fertility/disease before clearing the plot. | Unit planting, seed reservation, maturity timestamp, harvest yield and plot-reset tests; Playwright farm start/cancel test. |
| Legacy location actions | Action button / `act` | Applies validated HP/文 effects atomically through the wallet ledger without endurance costs or observation cultivation rewards, while recording the matching action-template identity. | Unit legacy-action compatibility/free-action and Playwright seed-tour tests. |
| World chat | Chat form / `chat.send` | Normalizes, rate-limits, persists, and broadcasts 1–120 character messages. | Unit chat, live smoke, and Playwright multiplayer tests. |
| Neighborhood-scoped online presence | WebSocket lifecycle | Deduplicates players and keeps the global online count exact, but sends identity/position records only for players inside each recipient’s current skill-scaled neighborhood. Socket visibility scopes refresh on sync, movement and completed timed actions. | Unit distant-player filtering and Playwright multiplayer movement/presence tests. |
| Responsive game workspace | Desktop panels / mobile navigation | Keeps headings, external map status, a pannable map stage and direction footer in document flow; wraps action text, scrolls dense character/chat content internally, prevents page-level horizontal overflow, and opens world/action/character/chat drawers on mobile. | Playwright desktop map-layout and 390×844 overflow/drawer tests. |

## Map design functions

| Function | Interface | Behavior | Automated coverage |
| --- | --- | --- | --- |
| Region editing | Drag palette, canvas, inspector | Creates, drags, resizes, updates, and safely soft-deletes large region boxes. | Unit operation tests and Playwright region-drag test. |
| Grid location editing | Drag palette and location boxes | Snaps to the nearest unique grid cell, assigns a containing region, auto-adds an Observe action, and auto-saves. | Unit collision test and Playwright HTML5-drop test. |
| Direction slots | Normal-route editor | Atomically reserves reciprocal direction slots; each location has at most one route per direction. | Unit direction-slot test and Playwright route test. |
| Edit leases | Automatic scope acquisition / Finish Editing | Locks regions or public chunks only when a drag changes coordinates or a save mutates data; selection-only clicks remain lock-free. Leases renew every30 seconds, expire after two minutes, and block other players. | Unit expiry/exclusion and Playwright selection-without-lock/contention tests. |
| Undo/redo | Editor toolbar / map history protocol | Stores the latest50 inverse operations within the active edit lease. | Unit save/undo/redo and Playwright history test. |
| Indexed chunk loading | Viewport controls / `map.viewport.subscribe` | Loads the exact requested 9/25/49 chunk keys through the composite `(layer,active,chunkX,chunkY,id)` index, returns aggregates at low zoom, and caps detailed responses at1200 locations. | Unit payload-cap/index-plan test and 50k benchmark. |
| Incremental synchronization | `map.chunks.invalidated` | Broadcasts affected chunk keys; clients reload only bounded local data. | Playwright editor-to-game visibility test. |
| Safe game-schema upgrades | SQLite startup migration | Preserves legacy IDs and user content through schema upgrades; schema v10 adds wallets/retail, v11 derivatives, v12 resident systems, v13 scoped agents/market/law, v14 repairs legacy `silverDelta`, v15 adds player editor roles plus immutable action-job rule versions/snapshots, and v16 adds external OIDC subject bindings. Existing jobs retain a compatibility fallback while every new job snapshots its accepted rule. | Unit v9→v10 currency idempotency, schema-v11/v12/v13 invariants, schema-v13 repair/reopen idempotency, schema-v15 snapshot/cost and v16 external-identity behavior, legacy-column isolation and populated-schema regressions. |
| Flat seeded-map storage | `map_layers`, layer-scoped chunks and editor map selector | Keeps all public travel on one `world-root`; the modern house, five public Kaifeng complexes and eight representative shops are separate single-floor maps directly under that root, never nested into floors or district hierarchies. | Unit fifteen canonical-map inventory, viewport and transition tests; Playwright map inventory. |
| Exceptional-map editing | Editor map forms / `map.edit` | Creates, renames, reparents and deletes empty custom interior/special maps under edit leases; rejects self/descendant cycles and protects initial maps. | Unit layer lifecycle/cycle test and Playwright map-create/update/delete test. |
| Cross-map target search | Editor connection form / `map.locations.search` | Searches at most 200 active locations in a selected map, then creates typed door/stairs/elevator/gate/road/ferry/dungeon/fast-travel/portal connections without using direction slots. | Unit bounded location-search/transition test and Playwright cross-map connection test. |
| Coordinate-disambiguated location controls | Editor quick locate / connection target selects | Formats every selectable location as `完整名称 · (网格X, 网格Y)`, so repeated road names remain individually addressable while IDs stay authoritative. | Unit duplicate-name search and Playwright repeated-road option/selection tests. |
| Responsive editor workspace | Editor tools / canvas / inspector | Uses internally scrolling side panels on wide screens and a single-column, page-contained workspace at 1050px and below; its map canvas pans internally on narrow screens. Compact 120×80 rectangular editor locations contain only concise labels while the full selection name, grid coordinates, loaded chunks and zoom appear outside the SVG. | Playwright node-size/concise-label, selection-summary and 768px overflow test. |
| Large-map locate and render culling | Editor quick-locate form / SVG canvas | Searches up to 200 locations by partial name on the current map, jumps directly to the selected location’s chunk, and selects it for inspection. The SVG renders only regions and locations intersecting the visible world bounds plus connected edge routes, while retaining all loaded records for editing and connection forms. | Playwright distant-location jump and loaded-vs-rendered DOM-count test. |
| Source-tracked complete demo world | SQLite startup / `npm run map:seed` | Idempotently installs 1,050 active locations and 1,100 routes: 26 home/construction-source locations, 935 public Kaifeng locations and 89 single-floor interior locations. Seed revision 10 retires Palos while preserving revision-0 user content and full reachability from玄关. | Unit idempotent import, Palos relocation, historical-city/shop/building anchors, deterministic counts and source tests. |
| Map integrity validation | `npm run map:validate` | Checks 15 canonical maps, no active Palos location, construction endpoint, 120 storefronts, 89 building locations, sources, eight-direction slots, 14 seeded interior transitions, Kaifeng span/loops, hubs, training room and full reachability. | Unit source-tracked Kaifeng/shop/building world validation test; command exits non-zero on errors. |

## Action-rule design functions

| Function | Interface | Behavior | Automated coverage |
| --- | --- | --- | --- |
| Structured rule CRUD | `/action-editor` / `rules.actions.list`, `rules.action.create/update/delete` | Edits all template metadata, requirements, checks, costs, success/failure outcomes, text, action duration, skill-effect duration, cooldown, target, adult flag and visibility through bounded Zod structures with optimistic versions and soft deletion. Referenced item definitions and cost direction are validated before persistence; unsaved local drafts survive remote invalidations, the socket reconnects automatically, the responsive page remains human-scrollable, and production mutations require editor/admin authorization. | Unit create/version/conflict/update/delete/reference/cost-validation tests and Playwright scroll/reconnect/draft/rule-duration/lifecycle coverage. |
| Adult-rule guardrails | Rule save | Requires every adult rule to target a player, be participants-only, and require same-location online targets; adult/targeted rules cannot become location shortcuts and remain executable only through per-action consent. | Unit unsafe-adult rejection and Playwright existing consent flow. |
| Location/facility bindings | Location search / `rules.location.inspect`, `rules.binding.upsert/delete` | Searches existing maps, inspects full location/facility data, and upserts prioritized rule bindings. Game clients receive `rules.invalidated` and refresh available actions. | Unit bind/availability/remove tests and Playwright create-bind-game-execute test. |
| Persistent seed overrides | Rule/template or binding edit | Any edited seeded row becomes `seed_revision=0`; later idempotent world/action seeding updates only still-seeded rows, preserving player overrides and soft deletions. | Unit edit-then-reseed preservation test. |

## Callable protocol messages

| Direction | Messages |
| --- | --- |
| Browser/gameplay client → server | `sync`, `move`, `travel.fast`, `act` (legacy), `action.start/cancel/queue.reorder`, `qinggong.start`, `skill.use/stop`, `shop.inspect/buy/sell`, `market.snapshot`, `market.order.place/cancel`, `person.inspect`, `person.dialogue.choose`, `commission.accept/complete/abandon`, `law.surrender`, `profile.adult.update`, `interaction.greet/request/respond`, `relationship.end`, `player.block`, `trade.request/respond/offer/confirm/cancel`, `combat.start/choose/respawn`, `loot.take`, `inventory.equip/unequip/use`, `craft.start`, `farm.start`, `attributes.allocate`, `cultivation.breakthrough`, `chat.send`, `ping`, `rules.actions.list`, `rules.location.inspect`, `rules.action.create/update/delete`, `rules.binding.upsert/delete`, `map.visited`, `map.viewport.subscribe`, `map.locations.search`, `map.lock.acquire/renew/release`, `map.edit`, `map.history.undo/redo` |
| Server → browser/gameplay client | `snapshot`, private gameplay-agent-only `npc.directive`, `ack`, `self.updated`, `action.updated`, `inventory.updated`, `shop.snapshot`, `market.snapshot`, `market.updated`, `person.snapshot`, `person.dialogue.result`, `commissions.updated`, `social.updated`, `cultivation.updated`, `players.updated`, `world.event`, `chat.message`, `world.updated`, `rules.actions.snapshot`, `rules.location.snapshot`, `rules.invalidated`, `map.visited.snapshot`, `map.viewport.snapshot`, `map.locations.result`, `map.edit.session`, `map.history.state`, `map.chunks.invalidated`, `map.locks.updated`, `error`, `pong` |
| Market-trade agent ↔ server | Client: `agent.trade.strategy.get/install`, `agent.trade.context.create`, `agent.trade.decision.submit`, `ping`. Server: `agent.trade.strategy`, `agent.trade.context`, `agent.trade.decision.result`, `pong`, `error`. All other messages are rejected. |

## Operations and verification functions

| Command | Function |
| --- | --- |
| `npm run dev` | Runs the custom Next.js and WebSocket development server. |
| `npm run build` | Builds the Next.js application and custom server. |
| `npm start` | Runs the production custom server. |
| `npm run npc:start` | Runs the separately built persistent NPC WebSocket actor process; configure `NPC_COUNT`, `NPC_SERVER_URL`, `NPC_RUNNER_SECRET`, or `NPC_CONTROLLER_URL`. |
| `npm run npc:dev` | Runs the separate NPC actor process directly from TypeScript during development. |
| `npm run npc:trade:start` | Runs the separately built restricted NPC commodity-trade agents; configure `NPC_TRADE_COUNT`, `NPC_TRADE_INTERVAL_MS`, `NPC_SERVER_URL`, `NPC_TRADE_RUNNER_SECRET`, or optional `NPC_TRADE_STRATEGY_URL`. |
| `npm run npc:trade:dev` | Runs restricted NPC trade agents directly from TypeScript. |
| `npm run npc:trade:replay -- --decision=<id>` | Opens SQLite in query-only/read-only mode, validates the stored strategy/input/output hashes and deterministic decision, and never migrates, seeds, checkpoints or submits an order. |
| `npm run lint` | Runs ESLint across project source, configuration, scripts and tests while excluding generated Next.js, server-build and Playwright-report artifacts. |
| `npm test` | Runs SQLite, game, direction, editor, lock, history, and chunk unit tests. |
| `npm run test:e2e` | Builds and runs the production-backed Playwright game/editor suite, then deletes its temporary SQLite DB/WAL/SHM files through global teardown. |
| `npm run test:all` | Runs lint, TypeScript, read-only map validation, unit tests and the production-backed Playwright suite. |
| `npm run smoke:live` | Verifies two-client chat and movement against a running server. |
| `npm run db:backup` | Creates an online SQLite backup. |
| `npm run map:seed` | Idempotently imports the bundled source-tracked modern house and Northern Song Kaifeng world, retires obsolete bundled regions, validates the result, and prints a JSON report. |
| `npm run map:validate` | Opens SQLite in query-only/read-only mode and validates the active world topology, provenance, minimum location counts and reachability without migration, seeding or checkpoint writes. |
| `npm run map:seed-load -- --locations=50000` | Generates repeatable large-map data and reports the exact 49-chunk viewport query time and SQLite index plan. Use a temporary `DATABASE_PATH` for disposable benchmarks. |
