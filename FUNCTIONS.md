# Project Functions

This is the canonical inventory of implemented project behavior. Update it in the same change whenever a function is added, changed, renamed, or removed.

## Application entries

| Entry | Function | Persistence / real-time | Automated coverage |
| --- | --- | --- | --- |
| `/` | Loads the shared-world game from玄关 and renders China time, a text-only rectangular-node local movement view, a button-driven personal exploration map, real-time facility actions and queue, needs, attribute/combat/cultivation status, and chat. | Reads a local SQLite neighborhood from the 1,300+ location source-tracked world and authoritative progression; settles offline needs/actions and requests the larger per-player visited map on demand over `/ws`. | Unit world validation/progression/exploration/action tests and Playwright continuous-world tour, action queue, visited-map, direction-control, multiplayer, responsive-layout, and mobile tests. |
| `/map-editor` | Loads the responsive collaborative drag/drop designer for the continuous overworld and exceptional maps, with a concise canvas-status bar, text-only rectangular locations, chunk controls, inspector, locks, and history. | Reads bounded map viewports and writes through authenticated WebSocket commands. | Playwright two-seed-map inventory, responsive layout, drop, route, undo/redo, region-drag, and lock-contention tests. |
| `/api/session` | Restores or creates a random player and HttpOnly browser session, then performs a same-origin redirect. | Writes `players`, `sessions`, and an arrival event. | Unit identity test and Playwright entry test. |
| `/ws` | Authenticates the session and runs game, chat, presence, map viewport, lock, editing, and history protocols. | Uses SQLite transactions and broadcasts incremental invalidations. | Live smoke and Playwright real-time/editor tests. |

## Player and world functions

| Function | Interface | Behavior | Automated coverage |
| --- | --- | --- | --- |
| Automatic identity | First browser visit | Creates a collision-safe random identity at `home-entrance`; browser contexts remain distinct. | Unit 2,000-name test and Playwright identity test. |
| China-standard time | World panel / `world.updated` | Displays an `Asia/Shanghai` `YYYY-MM-DD HH:mm:ss` clock synchronized from server time. | Unit deterministic-time and Playwright world tests. |
| Skill-scaled neighborhood | Game map | Queries only the current-map ordinary-route graph at base depth 3. A successful Eagle Eye action grants a persisted 30-minute skill-scaled bonus up to depth 8; the fitted viewport and scoped presence follow that depth, while temporarily seen locations never enter exploration history. | Unit Eagle Eye duration/range/exploration tests and Playwright temporary expanded-vision test. |
| Persistent exploration history | Successful movement / `map.visited` | Records the initial location and every successfully reached destination per opaque player in `player_visited_locations`. The on-demand query returns only active locations this player has visited, only connections whose endpoints were both discovered, and only maps containing those locations; schema v6 backfills existing players from movement logs without mixing identities. | Unit initial-map, discovered-route, revisit deduplication and per-player isolation tests; Playwright visited-map request test. |
| Personal visited-map viewer | Desktop “足迹地图” / mobile “足迹” buttons | Opens a read-only fitted overview of every location reached by the current player. It switches between visited maps, draws only discovered same-map connections, marks the current position, and exposes full names/areas/coordinates outside compact rectangular nodes; it never reveals or enables travel to undiscovered places. | Playwright initial secrecy, movement growth, map switching, route, inspection, close and mobile-button tests. |
| Concise map labels | Game and editor location rectangles | Derives a map-only label of at most nine characters by removing repeated house/region/category prefixes and compacting generated cave/memo names. Canonical full names remain unchanged in SQLite and commands; full names plus coordinates remain available through accessibility/status text, editor selection and inspector fields. | Unit exact-label/truncation tests and Playwright compact-node/full-status tests. |
| Geographic public overworld | `world-root` ordinary-route graph | Places the house exterior and three same-named 楼门路 segments beside an approximately 73×83-grid Northern Song region and 69×66-grid Palos region on one chunked map. The Song region uses 618 administrative/relay nodes positioned by the 1123 atlas plus 189 connecting road nodes; Palos normalizes 267 upstream marker coordinates and adds collision-safe connecting roads for 510 regional nodes. Repeated road names represent physical segments while stable IDs and coordinates distinguish them; ordinary movement crosses regions without overview/category-map transitions. | Unit exact anchor/marker coordinate, geographic-span, road-density, duplicate-name, flattened-topology and migration validation; Playwright Song-to-Palos geographic-road tour. |
| Eight-direction movement | Map box / `move` | Ordinary routes connect one adjacent grid cell in eight directions; movement and ordinary actions never check or consume endurance. | Unit free-movement/direction tests and Playwright seed-tour tests. |
| Explicit direction controls | Boxed “下一步” footer | Lists each currently adjacent direction and destination outside the SVG, disables movement while offline or pending, and sends the same validated `move` command as a rectangular map node. | Playwright rectangular-node and direction-control test. |
| Plain-HTTP browser compatibility | Game and map-editor commands | Generates collision-resistant client request/entity IDs with `crypto.getRandomValues` and a legacy fallback, so movement and editing work when `crypto.randomUUID` is unavailable. | Playwright no-`randomUUID` movement and editor tests. |
| Interior transitions and portals | Action panel / `move` | The bundled world uses one door transition between the overworld and the complete single-floor house. Doors, stairs and portals remain available for user-created exceptional maps; public Song and Palos travel uses ordinary routes. | Unit same-layer/cross-layer route tests and Playwright house/continuous-overworld tour. |
| Six-attribute progression | Character tabs / `attributes.allocate` | Stores strength, agility, constitution, root, comprehension and spirit; atomically spends permanent points and recalculates transparent combat values. | Unit formulas/scaling/atomic-allocation tests and Playwright allocation test. |
| Cultivation and breakthroughs | Training locations / `cultivation.breakthrough` | Settles unlimited online/offline cultivation from server time, auto-levels within a realm, and applies 45/60/75/100% breakthroughs with failure cost and 30% carry. | Unit clock/idempotency/failure/success/reward tests and Playwright training/offline/100% breakthrough tests. |
| Derived combat panel | Character “战斗属性” tab | Computes HP, endurance, min/max attack, defense, speed, hit, dodge, critical rate/damage and cultivation speed from attributes and realm. | Unit deterministic-formula and Playwright derived-panel tests. |
| Real-time action queue | Action panel / `action.start`, `action.cancel`, `action.queue.reorder` | Runs exactly one foreground action and up to eight ordered waiting actions with real-world durations. Server timestamps continue offline, complete in chronological order, advance the next job automatically, interrupt ordinary movement while active, and push one-second online completion refreshes without endurance costs. | Unit start/cap/reorder/cancel/movement-lock/server-clock/offline-completion tests and Playwright queue controls/countdown test. |
| Living needs | Action panel and player snapshot | Persists 0–100 satiety, hydration, hygiene, fatigue and bladder values; decays them by elapsed server hours and applies a visible 5% check penalty per poor need capped at 25%, without death or basic-movement costs. | Unit deterministic eight-hour decay, action recovery and maximum-penalty tests; Playwright needs summary test. |
| Reusable facility actions | Seed revision 6 / location facilities | Gives every active location observation and listening, maps every existing room to reusable wardrobe, kitchen, water, dining, toilet, bath, bed, study, training, farm, workshop, storage or social facilities, and tags Song settlements/markets/roads plus Palos wilderness landmarks. Templates expose duration, success probability, requirements and outcome summaries outside the location node. | Unit facility availability, skill experience and seeded action-catalog tests; Playwright location-action execution test. |
| Private perception results | `action.start` observation/listening/Eagle Eye | Observation returns the current description and facilities; listening returns at most three recent nearby public non-adult action traces or a quiet result. Results persist only in the actor's `private_events`, appear in the personal action history, and never broadcast later private/adult records. | Unit visibility/filter/result tests and Playwright private-result test. |
| Timed Qinggong travel | Action panel / `qinggong.start` | Lists only existing active same-layer locations two to a skill-scaled seven grid cells away on one of eight straight directions. Duration is `15 + 10 × grid distance` seconds, routes are not required, and movement plus exploration history occur only after a successful settlement; failure applies configured HP/fatigue costs. | Unit geometry/range/timing/success/failure/exploration tests and Playwright completed Qinggong test. |
| Complete item instances and equipment | Character “物品装备” tab / `inventory.equip`, `inventory.unequip`, `inventory.use` | Persists stackable resources and unique 1–5-quality item instances with binding, affixes, durability and equipment slots. Stable bound starter clothing, boots, weapon, tool, food and water are idempotently granted to existing/new actors; equipped effects modify derived combat stats, consumables atomically change needs/HP, and action-reserved quantities cannot be used or equipped. | Unit starter-idempotency, equip/stat, consumable and reservation tests; Playwright inventory controls test. |
| Recipe crafting | Current-facility recipe cards / `craft.start` | Exposes only recipes supported by the current kitchen/workshop/smithy, displays inputs, outputs, difficulty and real duration, reserves stack quantities when queued, releases them on cancel, and consumes them atomically before granting success output and skill XP. | Unit 30-minute server-clock crafting, cancel-release, input-consumption and stack-output tests; Playwright recipe visibility test. |
| Persistent real-time farming | Garden farm cards / `farm.start` | Gives each seeded farm facility a persistent plot. Planting reserves one seed for a 30-minute action, crops then grow independently for realistic 30/120-day periods, watering improves plot state, and a 30-minute mature harvest grants yield based on water/fertility/disease before clearing the plot. | Unit planting, seed reservation, maturity timestamp, harvest yield and plot-reset tests; Playwright farm start/cancel test. |
| Legacy location actions | Action button / `act` | Applies validated HP/silver effects atomically without endurance costs or observation cultivation rewards, while recording the matching schema-v7 action-template identity for forward compatibility. | Unit legacy-action compatibility/free-action and Playwright seed-tour tests. |
| World chat | Chat form / `chat.send` | Normalizes, rate-limits, persists, and broadcasts 1–120 character messages. | Unit chat, live smoke, and Playwright multiplayer tests. |
| Neighborhood-scoped online presence | WebSocket lifecycle | Deduplicates players and keeps the global online count exact, but sends identity/position records only for players inside each recipient’s current skill-scaled neighborhood. Socket visibility scopes refresh on sync, movement and completed timed actions. | Unit distant-player filtering and Playwright multiplayer movement/presence tests. |
| Responsive game workspace | Desktop panels / mobile navigation | Keeps headings, external map status, a pannable map stage and direction footer in document flow; wraps action text, scrolls dense character/chat content internally, prevents page-level horizontal overflow, and opens world/action/character/chat drawers on mobile. | Playwright desktop map-layout and 390×844 overflow/drawer tests. |

## Map design functions

| Function | Interface | Behavior | Automated coverage |
| --- | --- | --- | --- |
| Region editing | Drag palette, canvas, inspector | Creates, drags, resizes, updates, and safely soft-deletes large region boxes. | Unit operation tests and Playwright region-drag test. |
| Grid location editing | Drag palette and location boxes | Snaps to the nearest unique grid cell, assigns a containing region, auto-adds an Observe action, and auto-saves. | Unit collision test and Playwright HTML5-drop test. |
| Direction slots | Normal-route editor | Atomically reserves reciprocal direction slots; each location has at most one route per direction. | Unit direction-slot test and Playwright route test. |
| Edit leases | Automatic scope acquisition / Finish Editing | Locks regions or public chunks only when a drag changes coordinates or a save mutates data; selection-only clicks remain lock-free. Leases renew every30 seconds, expire after two minutes, and block other players. | Unit expiry/exclusion and Playwright selection-without-lock/contention tests. |
| Undo/redo | Editor toolbar / map history protocol | Stores the latest50 inverse operations within the active edit lease. | Unit save/undo/redo and Playwright history test. |
| Indexed chunk loading | Viewport controls / `map.viewport.subscribe` | Loads the exact requested 9/25/49 chunk keys through the composite `(layer,active,chunkX,chunkY,id)` index, returns aggregates at low zoom, and caps detailed responses at1200 locations. | Unit payload-cap/index-plan test and 50k benchmark. |
| Incremental synchronization | `map.chunks.invalidated` | Broadcasts affected chunk keys; clients reload only bounded local data. | Playwright editor-to-game visibility test. |
| Safe game-schema upgrades | SQLite startup migration | Upgrades populated legacy databases despite SQLite `ALTER TABLE` foreign-key limits, rebuilds schema v5 without the former global location-name uniqueness constraint, adds schema-v6 exploration history, and installs schema-v7 action, needs, skills, items, farming, relationship, combat and NPC storage while preserving IDs, legacy actions, foreign-key references and user-created records. New human identities default to unknown adult status with adult content disabled. | Unit populated-v1/v3 metadata upgrade, schema-v6 backfill, schema-v7 table/default/legacy-action, duplicate-name, visited-map and foreign-key regression tests. |
| Flat seeded-map storage | `map_layers`, layer-scoped chunks and editor map selector | Keeps only one public `world-root` and one complete `home-ground` single-floor house in the bundled world. Ordinary grid occupancy remains unique per map, while user-created interiors and special maps remain supported. | Unit revision-5 geographic flattening, viewport and transition tests; Playwright two-map inventory. |
| Exceptional-map editing | Editor map forms / `map.edit` | Creates, renames, reparents and deletes empty custom interior/special maps under edit leases; rejects self/descendant cycles and protects initial maps. | Unit layer lifecycle/cycle test and Playwright map-create/update/delete test. |
| Cross-map target search | Editor connection form / `map.locations.search` | Searches at most 200 active locations in a selected map, then creates typed door/stairs/elevator/gate/road/ferry/dungeon/fast-travel/portal connections without using direction slots. | Unit bounded location-search/transition test and Playwright cross-map connection test. |
| Coordinate-disambiguated location controls | Editor quick locate / connection target selects | Formats every selectable location as `完整名称 · (网格X, 网格Y)`, so repeated road names remain individually addressable while IDs stay authoritative. | Unit duplicate-name search and Playwright repeated-road option/selection tests. |
| Responsive editor workspace | Editor tools / canvas / inspector | Uses internally scrolling side panels on wide screens and a single-column, page-contained workspace at 1050px and below; its map canvas pans internally on narrow screens. Compact 120×80 rectangular editor locations contain only concise labels while the full selection name, grid coordinates, loaded chunks and zoom appear outside the SVG. | Playwright node-size/concise-label, selection-summary and 768px overflow test. |
| Large-map locate and render culling | Editor quick-locate form / SVG canvas | Searches up to 200 locations by partial name on the current map, jumps directly to the selected location’s chunk, and selects it for inspection. The SVG renders only regions and locations intersecting the visible world bounds plus connected edge routes, while retaining all loaded records for editing and connection forms. | Playwright distant-location jump and loaded-vs-rendered DOM-count test. |
| Source-tracked complete demo world | SQLite startup / `npm run map:seed` | Idempotently installs 25 sourced house/entrance/street locations, an 807-node Northern Song region (618 administrative/relay plus 189 atlas-road nodes), and a 510-node Palos region derived from 267 upstream markers and generated roads. Stable IDs, source URL/version/retrieval date, one continuous public layer, one 21-place single-floor house, revision-6 facilities/actions and full reachability from玄关 are preserved while retaining revision-0 user content. | Unit idempotent import, legacy hierarchy/schema upgrade, exact geographic coordinate, deterministic-count/action-catalog and `validateWorldMap` topology/source tests. |
| Map integrity validation | `npm run map:validate` | Checks layer cycles, the two canonical seed maps, public-location flattening, source records, eight-direction adjacency, reciprocal direction slots, the sole seeded interior transition, Song/Palos minimum geographic spans and road counts, training-room effect, per-world minimums and full reachability. | Unit source-tracked geographic-world validation test; command exits non-zero on errors. |

## Callable protocol messages

| Direction | Messages |
| --- | --- |
| Client → server | `sync`, `move`, `act` (legacy), `action.start/cancel/queue.reorder`, `qinggong.start`, `inventory.equip/unequip/use`, `craft.start`, `farm.start`, `attributes.allocate`, `cultivation.breakthrough`, `chat.send`, `ping`, `map.visited`, `map.viewport.subscribe`, `map.locations.search`, `map.lock.acquire/renew/release`, `map.edit`, `map.history.undo/redo` |
| Server → client | `snapshot`, `ack`, `self.updated`, `action.updated`, `inventory.updated`, `cultivation.updated`, `players.updated`, `world.event`, `chat.message`, `world.updated`, `map.visited.snapshot`, `map.viewport.snapshot`, `map.locations.result`, `map.edit.session`, `map.history.state`, `map.chunks.invalidated`, `map.locks.updated`, `error`, `pong` |

## Operations and verification functions

| Command | Function |
| --- | --- |
| `npm run dev` | Runs the custom Next.js and WebSocket development server. |
| `npm run build` | Builds the Next.js application and custom server. |
| `npm start` | Runs the production custom server. |
| `npm run lint` | Runs ESLint across project source, configuration, scripts and tests while excluding generated Next.js, server-build and Playwright-report artifacts. |
| `npm test` | Runs SQLite, game, direction, editor, lock, history, and chunk unit tests. |
| `npm run test:e2e` | Builds and runs the production-backed Playwright game/editor suite. |
| `npm run test:all` | Runs unit and Playwright suites. |
| `npm run smoke:live` | Verifies two-client chat and movement against a running server. |
| `npm run db:backup` | Creates an online SQLite backup. |
| `npm run map:seed` | Idempotently imports the bundled source-tracked house, Northern Song and Palworld world, validates it, and prints a JSON report. |
| `npm run map:validate` | Validates the active SQLite world topology, provenance, minimum location counts and reachability. |
| `npm run map:seed-load -- --locations=50000` | Generates repeatable large-map data and reports the exact 49-chunk viewport query time and SQLite index plan. Use a temporary `DATABASE_PATH` for disposable benchmarks. |
