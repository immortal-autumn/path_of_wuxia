# 贡献指南

感谢参与云州江湖志。项目仍是 demo，提交前请优先保证规则、持久化和多用户行为可复现。

## 开发流程

1. 创建一个描述清楚的分支，保持提交小而可回滚。
2. 使用 Node.js 24 LTS，执行 `npm ci` 安装依赖。
3. 修改用户可见功能、路由、WebSocket 命令、游戏规则、持久化行为或测试入口时，同时更新根目录的 `FUNCTIONS.md`。
4. 为行为变化添加或更新单元测试；跨页面、实时通信或响应式交互请补充 Playwright 测试。
5. 提交前运行下面的检查：

   ```bash
   npm run lint
   npx tsc --noEmit
   npm run map:validate
   npm test
   npm run test:e2e
   npm run build
   npm audit --omit=dev
   ```

不要把 `data/*.db`、备份、`.env`、构建产物、测试报告、凭证或本地 IDE 配置提交到仓库。

## 代码约定

- SQLite 是权威状态；跨钱包、物品、库存或关系的改动必须放在同一个事务内。
- 客户端只发送公开协议中的命令，服务端必须重新校验角色、地点、权限、资源和请求编号。
- 请求编号和幂等键应保持稳定；重复请求不得重复扣款、发奖或移动。
- 地图普通路线只使用八方向相邻格；跨层移动必须使用明确的过渡类型。
- 面向用户的文本使用简明中文，避免将内部枚举、稳定 ID 或控制器信息展示给玩家。
- 模态框、抽屉和地图控件需要支持键盘操作、移动端滚动和清晰的错误反馈。

## 提交与问题

提交信息建议使用简短的动词开头，例如 `fix: prevent duplicate shop charge` 或 `docs: clarify local setup`。提交 issue 时附上复现步骤、浏览器/Node 版本、相关日志和是否使用全新数据库；不要上传真实玩家数据、密钥或完整数据库文件。

安全问题请通过私下渠道联系维护者，确认修复前不要公开完整利用细节。
